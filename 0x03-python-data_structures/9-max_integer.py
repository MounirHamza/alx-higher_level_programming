#!/usr/bin/python3
def max_integer(my_list=[]):
    x = my_list[0]
    for i in range (len(my_list)):
        if my_list = "":
            return 0
        else my_list[i] > x:
            x = my_list[i]
            
    return x
